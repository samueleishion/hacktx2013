#!/bin/bash
# r --slave -f regression.r
#
file = 
df = read.table(file, header = T, sep = "\t")
x1 = df$
sentiment_P = df$
sentiment_N = 
sentiment_E = df


regress_adjP = cor(x, y, method = "pearson")
regress_adjN = cor(x, y, method = "pearson")
regress_adjE = cor(x, y, method = "pearson")

